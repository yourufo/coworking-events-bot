# Секреты и локальные настройки — никогда не попадают в git
.env

# Файл базы данных — это локальные данные, а не код;
# у каждого окружения (разработка/сервер) своя БД
data/*.db
data/*.db-journal

# Стандартный мусор Python
__pycache__/
*.pyc
.venv/
venv/
